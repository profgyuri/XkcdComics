<Window x:Class="XkcdComics.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XkcdComics.UI"
        mc:Ignorable="d" Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid
        Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="prevButton" Click="prevButton_Click" Content="Previous" HorizontalAlignment="Center"/>
        <Button x:Name="nextButton" Click="nextButton_Click" Content="Next" HorizontalAlignment="Center" Grid.Column="1"/>

        <Image x:Name="comicImage" Grid.Row="1" Grid.ColumnSpan="2" Margin="0 10 0 0"/>

    </Grid>
</Window>
